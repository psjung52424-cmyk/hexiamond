# Hexiamond Arena — AI 최적화 프롬프트 (GDD)

이 문서는 **AI에게 그대로 건네면 이 게임이 재현되도록** 쓰인 설계 문서다.
읽는 대상이 사람이 아니라 코드 생성 AI이므로, 취향이 아니라 **판정 가능한 규칙**으로 적었다.
아래 `프롬프트 본문` 전체를 복사해 AI에게 주면 된다.

- 구현체: [`index.html`](./index.html) — 의존성 없는 단일 파일
- 실행: https://psjung52424-cmyk.github.io/hexiamond/

---

## 프롬프트 본문

### 0. 과제

의존성 없는 **단일 HTML 파일**로 아래 사양의 퍼즐 게임을 구현하라.
외부 스크립트·이미지·음원 파일을 쓰지 말고, CSS와 JS를 모두 인라인하라.
`index.html` 하나를 브라우저로 열면 즉시 실행되어야 하고, 정적 호스팅에 그대로 올라가야 한다.

### 1. 한 줄 정의

정육각형 보드를 정삼각형으로 타일링하고, 그 위에 삼각형 조각을 놓아 **세 방향의 줄**을 지우는 퍼즐.
테트리스에서 출발했으나 **중력이 없고**, 조각은 보드 어디에나 놓인다.

### 2. 설계 원칙

구현 중 판단이 갈릴 때 아래 원칙을 기준으로 삼아라. 각 원칙에는 그렇게 정한 이유가 붙어 있다.

| 원칙 | 이유 |
|---|---|
| **중력을 넣지 않는다** | 낙하 처리를 없애면 판단이 "언제 놓을까"에서 "어디에 놓을까"로 옮겨간다. 삼각 격자에서는 후자가 훨씬 흥미롭다. |
| **조각을 회전시킬 수 없다** | 회전을 허용하면 대부분의 조각이 어디든 들어가 난이도가 무너진다. 방향은 스폰 시 무작위로 정해져 고정된다. |
| **시간을 재지 않는다** | 킬링타임 게임을 지향한다. 초 단위로 흐르는 숫자는 쫓기는 감각을 만들어 목적과 충돌한다. |
| **조각은 한 번에 하나만 보여준다** | 여러 개를 늘어놓으면 조합 최적화 문제가 되어 한 수의 무게가 가벼워진다. 다음 조각 1개만 예고한다. |
| **압박은 시간이 아니라 공간으로 준다** | 놓을 자리가 줄어드는 것 자체가 긴장이다. 남은 자리 수를 경고로 드러낸다. |

### 3. 보드와 좌표계

정육각형을 한 변 `N = 4`로 잡아 정삼각형 **96칸**으로 타일링한다.

칸을 `(r, g)`로 식별한다. `r`은 행, `g`는 행 안에서의 전역 열이다.

```
위를 향한 삼각형(▲)  ⟺  (r + g) 가 홀수
```

행 구성 — `r = 0 … 2N-1`에 대해 `d = min(r, 2N-1-r)`일 때

```
행의 칸 수     w(r) = 2(N + d) + 1
행의 시작 열   off(r) = N - d - 1        →  g = off(r) … off(r)+w(r)-1
```

`N = 4`이면 행별 칸 수는 `9 · 11 · 13 · 15 · 15 · 13 · 11 · 9`, 합 96.

**세 개의 라인 축.** 각 칸은 세 방향의 줄에 동시에 속한다.

```
A = r                  (가로)
B = ⌊(g - r) / 2⌋      (↗ 대각)
C = ⌈(g + r) / 2⌉      (↘ 대각)
```

세 축 모두 `2N`개의 줄을 가지며, 줄 길이 분포가 행 구성과 완전히 같다.
`N = 4`이면 축마다 8줄, 총 **24줄**. 구현 후 이 대칭성을 반드시 검산하라.

**화면 좌표.** 삼각형 한 변을 `S`, 높이를 `H = S·√3/2`라 할 때

```
▲(r,g) 꼭짓점: (gS/2, (r+1)H), ((g+2)S/2, (r+1)H), ((g+1)S/2, rH)
▼(r,g) 꼭짓점: (gS/2, rH),     ((g+2)S/2, rH),     ((g+1)S/2, (r+1)H)
무게중심:      x = S(g+1)/2,   y = H(r + 2/3)  [▲] / H(r + 1/3)  [▼]
```

### 4. 조각

정삼각형을 변끼리 이어 붙인 **폴리아이아몬드** 7종. 오른쪽은 등장 가중치.

| 칸 | 이름 | 기준 좌표 | 방향 | 가중치 |
|---|---|---|---|---|
| 1 | 삼각형 | `(0,1)` | 2 | 5 |
| 2 | 마름모 | `(0,1) (0,2)` | 3 | 15 |
| 3 | 사다리꼴 | `(0,1) (0,2) (0,3)` | 6 | 19 |
| 4 | 평행사변형 | `(0,1) (0,2) (0,3) (0,4)` | 3 | 16 |
| 4 | 큰 삼각형 | `(0,1) (1,0) (1,1) (1,2)` | 2 | 15 |
| 4 | 갈고리 | `(0,1) (0,2) (0,3) (1,3)` | 6 | 16 |
| 6 | **정육각형** | `(0,1) (0,2) (0,3) (1,1) (1,2) (1,3)` | 1 | 7 |

방향까지 세면 플레이어가 마주치는 **서로 다른 모양은 23가지**다.

1칸 조각은 막힌 판을 푸는 구제 카드다. 흔하면 게임이 무너지므로 가중치를 낮게 유지하라.
6칸 정육각형은 회전 대칭이라 방향이 하나뿐이며, 이 게임의 상징이다.

> **함정 — 4칸 조각은 세 종뿐이다.**
> 갈고리의 거울상 `(0,1) (0,2) (0,3) (1,1)` 을 별개 조각으로 추가하고 싶어지지만,
> 그것은 갈고리 자신의 회전이다. 따로 등록하면 같은 모양이 두 배 확률로 나온다.
> 조각을 추가할 때는 반드시 **모든 조각의 모든 방향을 모아 교차 중복이 없는지 검사하라.**

**60° 회전.** 플레이어는 회전할 수 없지만, 스폰 시 무작위 방향을 고르려면 회전 연산이 필요하다.
삼각형을 세 꼭짓점으로 분해해 육각 격자에서 회전시킨 뒤 삼각형으로 복원한다.

```
꼭짓점 (m, n) → 축 좌표 (i, j) = ((m-n)/2, n)
60° 회전:      R(i, j) = (-j, i + j)
복원:          (m, n) = (2i + j, j)
```

복원 시 세 꼭짓점 중 `n`이 같은 두 개를 찾아, 그 `n`이 나머지 하나보다 크면 ▲, 작으면 ▼다.
`g`는 그 두 꼭짓점의 `m` 중 작은 값.

**평행이동 주의.** `(dr + dg)`가 짝수일 때만 삼각형의 방향이 보존된다.
조각을 정규화할 때 이 패리티를 반드시 맞춰라. 홀수로 옮기면 ▲와 ▼가 뒤집혀 다른 조각이 된다.

**검증.** 각 조각의 6방향을 생성한 뒤 ① 변 인접 기준으로 연결되어 있는지 ② 칸 수가 보존되는지
③ **다른 조각과 같은 모양을 만들지 않는지** 확인하라.
정상이면 방향 수는 표 순서대로 `2, 3, 6, 3, 2, 6, 1`이고, 서로 다른 모양의 총합은 23이다.

### 5. 배치 규칙

- 중력 없음. 빈 칸이 맞기만 하면 어디에나 놓는다.
- **커서가 올라간 칸은 언제나 조각에 포함된다.** 조각의 각 칸을 차례로 커서 칸에 맞춰 보고,
  놓인 조각의 **무게중심이 커서에 가장 가까운** 배치를 고른다.
  고정된 기준칸 하나를 쓰면 조각이 커서 옆으로 치우쳐 보이므로 그렇게 하지 마라.
- 방향이 맞지 않는 기준칸(패리티 불일치)은 후보에서 제외한다.
- 놓을 수 없는 자리를 클릭하면 거부음을 낸다. 조용히 무시하지 마라.

### 6. 소멸과 점수

세 축 중 어느 줄이든 모든 칸이 차면 그 줄이 사라진다. 여러 줄이 동시에 완성될 수 있다.

```
배치 점수 = 놓은 삼각형 수
소멸 점수 = (지워진 칸 수) × 10 × 동시배수 × 콤보배수

동시배수 = k(k+1)/2        k = 동시에 지워진 줄 수  →  1, 3, 6, 10, 15
콤보배수 = 1 + 0.3(c - 1)  c = 연속으로 지운 횟수 (못 지우면 0으로 초기화)
```

동시 소멸을 압도적으로 유리하게 만드는 것이 의도다. 한 줄씩 지우는 플레이는 점수가 늘지 않는다.

### 7. 피드백

**시각.** 커서를 올리면 그 자리에 놓았을 때 **완성되는 줄을 해당 축의 색으로 미리 칠한다.**
3축 규칙을 설명 없이 가르치는 장치이므로 반드시 넣어라.

소멸 시 각 삼각형을 조각내 바깥으로 튕겨 회전·소멸시킨다. 2줄 이상이면 충격파와 짧은 화면 흔들림을 더한다.
**파편 수는 소멸 칸 수에 반비례해 줄여라** — 대량 소멸에서 프레임이 튄다.

**결정적 제약 — 소멸 시 입력을 막지 마라.** 지워지는 칸은 즉시 비우고, 사라지는 모습은
보드가 아니라 오버레이의 파편이 대신 보여준다. 애니메이션이 끝날 때까지 기다렸다가 비우면
매번 수백 밀리초씩 입력이 씹혀 플레이가 버벅인다.

**청각.** 외부 음원 없이 Web Audio API로 합성한다.

| 상황 | 소리 |
|---|---|
| 배치 | 짧은 저역 클릭 |
| 배치 불가 | 하강하는 둔탁음 |
| 소멸 | 펜타토닉 아르페지오 — 동시 줄 수만큼 음이 늘고, 콤보가 쌓이면 조가 올라간다 |
| 위험 진입 | 경고음 2회 (진입 순간 1회만) |
| 게임 오버 | 하강 4음 |

펜타토닉을 쓰는 이유는 어떤 조합으로 겹쳐도 불협이 생기지 않기 때문이다.
오디오 컨텍스트는 첫 사용자 조작 때 생성하고, 음소거 상태를 저장하라.

**공간 압박.** 현재 조각이 들어갈 수 있는 **서로 다른** 자리를 매 턴 세어라.
4곳 이하면 보드 테두리를 붉게 점멸시키고 경고 문구를 띄운다. 0곳이면 게임 오버.
같은 자리를 중복해서 세지 않도록 배치를 정규화해 중복을 제거하라.

### 8. 리더보드

Firebase Realtime Database에 **REST API로만** 접근한다. SDK를 로드하지 마라.

```
GET  <DB_URL>/scores.json          전체 조회 후 클라이언트에서 점수 내림차순 정렬
POST <DB_URL>/scores.json          { name, score, lines, moves, ts }
```

`DB_URL`을 파일 최상단에 **한 줄로 분리**하고, 비어 있으면 `localStorage` 기반 로컬 랭킹으로
자동 전환하라. UI는 두 모드에서 동일하게 동작해야 하며, 현재 모드를 배지로 표시한다.
네트워크 실패 시에도 로컬 기록으로 조용히 대체하고 화면이 깨지지 않아야 한다.

### 9. 타임랩스

배치할 때마다 `{ 놓은 칸, 누적 점수, 지워진 줄과 축 }`을 기보로 쌓는다.
재생하면 보드를 비운 뒤 그 순서대로 다시 채우며, 소멸 연출도 그대로 재현한다.

재생 속도는 **초당 4수 고정**(한 수당 250ms)이다. 판의 길이에 따라 간격을 조절하지 마라 —
길이에 맞춰 늘였다 줄이면 한 수의 리듬이 판마다 달라져 여러 판을 비교할 때 체감이 흔들린다.

**배속 조절 기능은 넣지 마라.** 이 기능의 목적은 자기 판을 되짚어 보는 것이지 빨리 넘기는 것이
아니다. 선택지를 주면 매번 무엇을 고를지 판단해야 해서 보는 일에 집중이 깨진다.

재생 중에는 진행한 수와 누적 점수, 진행 바만 보여준다. 재생이 끝나면 보드를 원래 상태로
정확히 복원하라.

### 10. 아트 디렉션

- **단일 테마(다크).** 배경·전경 색을 모두 명시해 뷰어 테마와 무관하게 일정하게 보여라.
- **바탕**은 푸른 기가 도는 잉크색. 순수 회색을 쓰지 마라.
- **놓인 조각**은 차가운 은회색 하나로 통일하되, ▲와 ▼의 명도를 달리해 결정면처럼 보이게 한다.
- **강조색은 세 축의 색으로만 쓴다.** 가로=앰버, ↗=민트, ↘=로즈.
  색이 곧 정보이므로 장식으로 쓰지 마라.
- **타이포**는 각진 표시용 서체와 본문용 한글 서체를 짝지어 쓴다.
  숫자는 자릿수가 흔들리지 않게 `tabular-nums`.
- 화면을 스크롤 없이 한눈에. 상단 지표 → 보드 → 조각 → 조작 순.

### 11. 완료 기준

아래를 모두 만족해야 완성이다.

1. 세 축의 줄 길이 분포가 `9·11·13·15·15·13·11·9`로 동일하다
2. 조각 8종의 모든 방향이 연결되어 있고 칸 수가 보존된다
3. 커서가 짚은 삼각형이 언제나 놓이는 조각에 포함된다
4. 라인 소멸 중에도 다음 조각을 즉시 놓을 수 있다
5. `DB_URL`을 지워도 로컬 랭킹으로 정상 동작한다
6. 타임랩스 재생 후 보드 상태가 재생 전과 정확히 같다
7. 파일이 하나뿐이고 외부 요청이 폰트와 리더보드 외에 없다

---

## 이 프롬프트를 쓰는 법

**한 번에 다 주지 마라.** 3장(좌표계)까지만 주고 보드가 정확히 그려지는지 먼저 확인하라.
삼각 격자는 패리티를 한 번 틀리면 그 위의 모든 것이 조용히 어긋난다.
좌표계가 검산을 통과한 뒤에 조각 → 배치 → 소멸 → 연출 순으로 얹는 것이 안전하다.

**2장의 원칙은 지우지 마라.** 사양만 남기면 AI가 "친절하게" 회전이나 타이머를 도로 넣는다.
왜 넣지 않았는지가 함께 있어야 그 결정이 유지된다.
